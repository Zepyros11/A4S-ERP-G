-- ============================================================
-- Migration 021: Add grace_days to events
-- จำนวนวันที่ให้เวลาผู้ลงทะเบียนชำระเงิน (default = null → ใช้ app_settings.default_grace_days)
-- ============================================================

ALTER TABLE events
  ADD COLUMN IF NOT EXISTS grace_days INTEGER;

-- ============================================================
-- DONE ✅
-- ============================================================
