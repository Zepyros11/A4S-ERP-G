-- ============================================================
-- Migration 044: detail column on event_tag_categories
-- ใช้แสดงในข้อความ LINE flex ตอน check-in สำเร็จ
-- (แทน list ชื่อ tag เปล่าๆ — ผู้รับจะเห็น detail ของ tag ที่ตัวเองมี)
-- ============================================================

ALTER TABLE event_tag_categories
  ADD COLUMN IF NOT EXISTS detail TEXT;

-- ============================================================
-- DONE ✅
-- ============================================================
