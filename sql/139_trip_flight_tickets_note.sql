-- ============================================================
-- Migration 139: trip_flight_tickets.note — หมายเหตุต่อ Ticket
--
-- Why:
--   แต่ละ Ticket (โซนตั๋วเครื่องบินในหน้า room-assign) เพิ่มหมายเหตุได้
--   เช่น "รอยืนยันที่นั่ง", "ขอ window", "เปลี่ยนเที่ยวบิน"
--   แก้ผ่านปุ่ม 📝 บน header ของ Ticket (PromptModal)
--
-- Idempotent — รันซ้ำได้
-- ============================================================

ALTER TABLE trip_flight_tickets ADD COLUMN IF NOT EXISTS note TEXT;

NOTIFY pgrst, 'reload schema';

-- ============================================================
-- DONE ✅
-- ============================================================
